# Private Material

Place here any files that should stay in the GitHub repository but should not be built into the public Jupyter Book website.

Typical use:

- notebooks for future years
- draft lectures
- alternate versions of a class
- internal notes

Everything under `private/` is excluded from the website build through `_config.yml`.
